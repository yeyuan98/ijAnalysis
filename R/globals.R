# Variable list for dplyr operations

utils::globalVariables(c(
  # selectNCT
  c("tp", "meas", "plane", "equi.D"),
  # RoiPointsSaver, python
  c("saveRoiPoint"),
  # nnd.export
  c("idx", "dist", "t.x", "t.y", "nnd", "spotIn.y")
))
